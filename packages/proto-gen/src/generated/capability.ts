// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v3.19.1
// source: capability.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./google/protobuf/timestamp";
import {
  CapabilityStatus,
  capabilityStatusFromJSON,
  capabilityStatusToJSON,
  CapabilityType,
  capabilityTypeFromJSON,
  capabilityTypeToJSON,
} from "./types";

export const protobufPackage = "clawbody.capability";

/** 能力元数据 */
export interface CapabilityMeta {
  id: string;
  name: string;
  version: string;
  type: CapabilityType;
  description: string;
  dependencies: string[];
}

/** 能力健康信息 */
export interface CapabilityHealth {
  status: CapabilityStatus;
  message: string;
  lastCheck: Date | undefined;
  details: { [key: string]: string };
}

export interface CapabilityHealth_DetailsEntry {
  key: string;
  value: string;
}

/** 操作描述符 */
export interface OperationDescriptor {
  name: string;
  description: string;
  /** JSON Schema as string */
  inputSchema: string;
  /** JSON Schema as string */
  outputSchema: string;
  streaming: boolean;
}

/** 能力信息 */
export interface CapabilityInfo {
  meta: CapabilityMeta | undefined;
  status: CapabilityStatus;
  operations: OperationDescriptor[];
}

function createBaseCapabilityMeta(): CapabilityMeta {
  return { id: "", name: "", version: "", type: 0, description: "", dependencies: [] };
}

export const CapabilityMeta: MessageFns<CapabilityMeta> = {
  encode(message: CapabilityMeta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    if (message.type !== 0) {
      writer.uint32(32).int32(message.type);
    }
    if (message.description !== "") {
      writer.uint32(42).string(message.description);
    }
    for (const v of message.dependencies) {
      writer.uint32(50).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityMeta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilityMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.dependencies.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapabilityMeta {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      type: isSet(object.type) ? capabilityTypeFromJSON(object.type) : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      dependencies: globalThis.Array.isArray(object?.dependencies)
        ? object.dependencies.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: CapabilityMeta): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.type !== 0) {
      obj.type = capabilityTypeToJSON(message.type);
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.dependencies?.length) {
      obj.dependencies = message.dependencies;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CapabilityMeta>, I>>(base?: I): CapabilityMeta {
    return CapabilityMeta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CapabilityMeta>, I>>(object: I): CapabilityMeta {
    const message = createBaseCapabilityMeta();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.version = object.version ?? "";
    message.type = object.type ?? 0;
    message.description = object.description ?? "";
    message.dependencies = object.dependencies?.map((e) => e) || [];
    return message;
  },
};

function createBaseCapabilityHealth(): CapabilityHealth {
  return { status: 0, message: "", lastCheck: undefined, details: {} };
}

export const CapabilityHealth: MessageFns<CapabilityHealth> = {
  encode(message: CapabilityHealth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.lastCheck !== undefined) {
      Timestamp.encode(toTimestamp(message.lastCheck), writer.uint32(26).fork()).join();
    }
    globalThis.Object.entries(message.details).forEach(([key, value]: [string, string]) => {
      CapabilityHealth_DetailsEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityHealth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilityHealth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lastCheck = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = CapabilityHealth_DetailsEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.details[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapabilityHealth {
    return {
      status: isSet(object.status) ? capabilityStatusFromJSON(object.status) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      lastCheck: isSet(object.lastCheck)
        ? fromJsonTimestamp(object.lastCheck)
        : isSet(object.last_check)
        ? fromJsonTimestamp(object.last_check)
        : undefined,
      details: isObject(object.details)
        ? (globalThis.Object.entries(object.details) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: CapabilityHealth): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = capabilityStatusToJSON(message.status);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.lastCheck !== undefined) {
      obj.lastCheck = message.lastCheck.toISOString();
    }
    if (message.details) {
      const entries = globalThis.Object.entries(message.details) as [string, string][];
      if (entries.length > 0) {
        obj.details = {};
        entries.forEach(([k, v]) => {
          obj.details[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CapabilityHealth>, I>>(base?: I): CapabilityHealth {
    return CapabilityHealth.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CapabilityHealth>, I>>(object: I): CapabilityHealth {
    const message = createBaseCapabilityHealth();
    message.status = object.status ?? 0;
    message.message = object.message ?? "";
    message.lastCheck = object.lastCheck ?? undefined;
    message.details = (globalThis.Object.entries(object.details ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseCapabilityHealth_DetailsEntry(): CapabilityHealth_DetailsEntry {
  return { key: "", value: "" };
}

export const CapabilityHealth_DetailsEntry: MessageFns<CapabilityHealth_DetailsEntry> = {
  encode(message: CapabilityHealth_DetailsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityHealth_DetailsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilityHealth_DetailsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapabilityHealth_DetailsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: CapabilityHealth_DetailsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CapabilityHealth_DetailsEntry>, I>>(base?: I): CapabilityHealth_DetailsEntry {
    return CapabilityHealth_DetailsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CapabilityHealth_DetailsEntry>, I>>(
    object: I,
  ): CapabilityHealth_DetailsEntry {
    const message = createBaseCapabilityHealth_DetailsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseOperationDescriptor(): OperationDescriptor {
  return { name: "", description: "", inputSchema: "", outputSchema: "", streaming: false };
}

export const OperationDescriptor: MessageFns<OperationDescriptor> = {
  encode(message: OperationDescriptor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.inputSchema !== "") {
      writer.uint32(26).string(message.inputSchema);
    }
    if (message.outputSchema !== "") {
      writer.uint32(34).string(message.outputSchema);
    }
    if (message.streaming !== false) {
      writer.uint32(40).bool(message.streaming);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OperationDescriptor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOperationDescriptor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.inputSchema = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.outputSchema = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.streaming = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OperationDescriptor {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      inputSchema: isSet(object.inputSchema)
        ? globalThis.String(object.inputSchema)
        : isSet(object.input_schema)
        ? globalThis.String(object.input_schema)
        : "",
      outputSchema: isSet(object.outputSchema)
        ? globalThis.String(object.outputSchema)
        : isSet(object.output_schema)
        ? globalThis.String(object.output_schema)
        : "",
      streaming: isSet(object.streaming) ? globalThis.Boolean(object.streaming) : false,
    };
  },

  toJSON(message: OperationDescriptor): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.inputSchema !== "") {
      obj.inputSchema = message.inputSchema;
    }
    if (message.outputSchema !== "") {
      obj.outputSchema = message.outputSchema;
    }
    if (message.streaming !== false) {
      obj.streaming = message.streaming;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OperationDescriptor>, I>>(base?: I): OperationDescriptor {
    return OperationDescriptor.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OperationDescriptor>, I>>(object: I): OperationDescriptor {
    const message = createBaseOperationDescriptor();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.inputSchema = object.inputSchema ?? "";
    message.outputSchema = object.outputSchema ?? "";
    message.streaming = object.streaming ?? false;
    return message;
  },
};

function createBaseCapabilityInfo(): CapabilityInfo {
  return { meta: undefined, status: 0, operations: [] };
}

export const CapabilityInfo: MessageFns<CapabilityInfo> = {
  encode(message: CapabilityInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.meta !== undefined) {
      CapabilityMeta.encode(message.meta, writer.uint32(10).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    for (const v of message.operations) {
      OperationDescriptor.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilityInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.meta = CapabilityMeta.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.operations.push(OperationDescriptor.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapabilityInfo {
    return {
      meta: isSet(object.meta) ? CapabilityMeta.fromJSON(object.meta) : undefined,
      status: isSet(object.status) ? capabilityStatusFromJSON(object.status) : 0,
      operations: globalThis.Array.isArray(object?.operations)
        ? object.operations.map((e: any) => OperationDescriptor.fromJSON(e))
        : [],
    };
  },

  toJSON(message: CapabilityInfo): unknown {
    const obj: any = {};
    if (message.meta !== undefined) {
      obj.meta = CapabilityMeta.toJSON(message.meta);
    }
    if (message.status !== 0) {
      obj.status = capabilityStatusToJSON(message.status);
    }
    if (message.operations?.length) {
      obj.operations = message.operations.map((e) => OperationDescriptor.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CapabilityInfo>, I>>(base?: I): CapabilityInfo {
    return CapabilityInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CapabilityInfo>, I>>(object: I): CapabilityInfo {
    const message = createBaseCapabilityInfo();
    message.meta = (object.meta !== undefined && object.meta !== null)
      ? CapabilityMeta.fromPartial(object.meta)
      : undefined;
    message.status = object.status ?? 0;
    message.operations = object.operations?.map((e) => OperationDescriptor.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
